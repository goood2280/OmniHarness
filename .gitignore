# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# ── Node ──
node_modules/
frontend/dist/
npm-debug.log*
.pnpm-store/

# ── OS / Editor ──
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Runtime data (per-deployment) ──
mission.json
reports/*.md
reports/*.json
!reports/.gitkeep

# Per-process session state (active project + last mode)
.active_project
.app_mode

# Per-project mission dirs
projects/

# Coordinator / viewer runtime state (REQ · ACT · BACKLOG · STATES · COST ledger)
backend/_state/

# ── Scratch ──
_scratch_*

# Local-only sprite-sheet backups from normalization passes
frontend/public/tiles/chars.pre-alpha-backup/
frontend/public/tiles/chars.pre-normalize-backup/
